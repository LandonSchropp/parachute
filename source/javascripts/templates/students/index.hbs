{{view ClassList.EditableListView}}

<p>
  <button {{action updateGroups}}>
    {{#if groupsEmpty}}
      Create the lists
    {{else}}
      Update the lists
    {{/if}}
  </button>
</p>

{{#if groupsEmpty}}

  <p class="instructions">Type in your students' names and press the button.</p>

{{else}}

  {{#each groups}}

    <h2>List {{index}}</h2>

    <table class="group-list">
      {{#each this}}
        <tr>
          {{#each this }}
           <td>{{name}}</td>
          {{/each }}
        </tr>
      {{/each}}
    </table>

  {{/each}}

{{/if}}